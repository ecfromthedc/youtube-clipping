use leptos::prelude::*;

#[component]
pub fn Pipeline() -> impl IntoView {
    view! { <div class="empty">"Pipeline — port in progress. Use the current UI at "<a href="/">"/"</a>" meanwhile."</div> }
}
