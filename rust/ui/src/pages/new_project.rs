use leptos::prelude::*;

#[component]
pub fn NewProject() -> impl IntoView {
    view! { <div class="empty">"New project — port in progress. Use the current UI at "<a href="/">"/"</a>" meanwhile."</div> }
}
