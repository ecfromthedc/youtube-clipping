use leptos::prelude::*;

#[component]
pub fn Dashboard() -> impl IntoView {
    view! { <div class="empty">"Projects — port in progress. Use the current UI at "<a href="/">"/"</a>" meanwhile."</div> }
}
